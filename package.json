{
  "name": "busted-grid",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18",
    "npm": ">=7"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "rimraf packages/*/dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  }
}
